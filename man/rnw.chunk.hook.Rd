% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnw.chunk.hook.R
\name{rnw.chunk.hook}
\alias{rnw.chunk.hook}
\title{Custom Knitr Chunk Hook for LaTeX Output}
\usage{
rnw.chunk.hook(x, options)
}
\arguments{
\item{x}{A character string containing the chunk content.}

\item{options}{A list of chunk options from \code{knitr}, specifying attributes such as font size,
background color, and output format.}
}
\value{
A character string with the LaTeX-formatted output, adjusted for font size
in code-only sections and customized colors.
}
\description{
Modifies the \code{knitr} chunk output in LaTeX documents, setting font size specifically
within the \code{kframe} environment rather than at the \code{knitrout} level. This ensures that
only the code portions are affected by the font size setting, while the text output remains independently controlled.
}
\details{
\itemize{
\item The function begins the \code{kframe} environment with the specified font size.
\item If \code{output_asis()} is \code{TRUE}, the output is directly inserted without additional formatting.
\item Font size for the code is controlled within \code{kframe}, while output text font size
remains unaffected, allowing separate control over text.
\item If \code{options$split} is \code{TRUE}, the output is saved as a separate \code{.tex} file, allowing for
later inclusion with \verb{\\\\input}.
}
}
\examples{
# Typical usage within a knitr document:
# knitr::opts_chunk$set(hook = rnw.chunk.hook)
# Set `options$size` to specify the code font size in LaTeX.
}
\keyword{internal}
